{
  "name": "runserver-match",
  "version": "1.0.0",
  "description": "MyRunnerGame Match Making Server",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "nodemon --exec ts-node src/index.ts"
  },
  "author": "Saint software",
  "license": "ISC",
  "dependencies": {
    "shelljs": "^0.8.4",
    "socket.io": "^2.3.0"
  },
  "devDependencies": {
    "@types/shelljs": "^0.8.8",
    "@types/socket.io": "^2.1.10"
  }
}
